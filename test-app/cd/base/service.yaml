apiVersion: v1
kind: Service
metadata:
  name: test-app
  labels:
    app.kubernetes.io/name: test-app
spec:
  selector:
    app.kubernetes.io/name: test-app
  ports:
    - protocol: TCP
      port: 80
